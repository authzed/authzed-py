#!/usr/bin/env -S buf generate
---
version: "v2"
inputs:
  - module: "buf.build/authzed/api:v1.35.0"
plugins:
  - local: "python"
    out: "."
  - local: "grpc_python_plugin"
    out: "."
  - local: "mypy"
    out: "."
  - local: "mypy_grpc"
    out: "."
